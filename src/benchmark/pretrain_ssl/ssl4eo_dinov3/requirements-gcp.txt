# Extra Python deps for the SSL4EO-S12 x DINOv3 pipeline on top of a PyTorch
# >= 2.7.1 base image (torch/torchvision come from the base image / conda).
# DINOv3's own deps are in ../dinov3/requirements.txt (installed via -e).
lmdb
numpy
omegaconf
scikit-learn
tensorboard
wandb
google-cloud-storage
rasterio        # extract_embeddings.py (GeoTIFFs) + build_lmdb.py (raw SSL4EO tiles)
opencv-python-headless  # build_lmdb.py: SSL4EO raw reader resizes bands via cv2
tqdm
